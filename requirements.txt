# Runtime dependencies shared by Docker, Vercel, and local execution.
# Keep this file at the project root: Dockerfile and @vercel/python both read it.

# FastAPI runtime
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# Validation and environment settings
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
email-validator>=2.2,<3.0

# Provider HTTP clients and uploads
httpx>=0.27,<1.0
python-multipart>=0.0.9,<1.0

# Data and authentication
motor>=3.6,<4.0
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0

# Background workers
celery[redis]>=5.4,<6.0
redis>=5.0,<6.0
